/**
 * Possible plugin types, see
 */
export enum PluginType {
  Completion = 'completion',
  Default = 'default',
  Grammar = 'grammar',
  Language = 'language'
}

/**
 * Apperance option values
 */
export enum Appearance {
  Auto = 'auto',
  Dark = 'dark',
  Light = 'light'
}

/**
 * Supported extensions
 */
export enum Extensions {
  Appearance = 'appearance'
}
